<%= form_tag submit_participant_admin_planned_tour_path, method: :put, class: 'add-participant-form formstatic' do |f| %>
  <%= select :participant, :customer_id, Customer.order(:name).map { |p| [ p.name, p.id ] }, { include_blank: "Select your participant" }, { class: "user-dropdown" } %>
  <%= submit_tag "Add participants", data: { disable_with: "Adding..." } %>
<% end %>